<div class="mag-main-menu" id="sticker">
    <div class="classy-nav-container breakpoint-off">
        <!-- Menu -->
        <nav class="classy-navbar justify-content-between" id="magNav">

            <!-- Nav brand -->
            <a href="/" class="nav-brand"><img src="/public/img/core-img/logo.png" width="50" height="50" alt=""></a>

            <!-- Navbar Toggler -->
            <div class="classy-navbar-toggler">
                <span class="navbarToggler"><span></span><span></span><span></span></span>
            </div>

            <!-- Nav Content -->
            <div class="nav-content d-flex align-items-center">
                <div class="classy-menu">

                    <!-- Close Button -->
                    <div class="classycloseIcon">
                        <div class="cross-wrap"><span class="top"></span><span class="bottom"></span></div>
                    </div>

                    <!-- Nav Start -->
                    <div class="classynav">
                        <ul>
                            <li class="active"><a href="/">Home</a></li>
                            <li><a href="archive.html">Archive</a></li>
                            <li><a href="#">Categories</a>
                                <ul class="dropdown">
                                    {{#each lcAllcatdetail as |alias|}}
                                        <li>
                                            <a href="/byCat/">{{CatName}}</a>
                                            <ul class="dropdown">
                                                {{#each lcAllSubCat as |alias|}}
                                                    {{#if (checkCat lcAllcatdetail.CatID lcAllSubCat.CatID)}}
                                                        <li>
                                                        <a href="">{{subCatName}}</a>
                                                    </li>
                                                    {{/if}}
                                                    
                                                {{/each}}
                                           </ul>
                                        </li>
                                    {{/each}}
                                </ul>
                            </li>
                            {{#if lcIsAuthenticated}}
                                <li><a href="/account/premium">premium</a></li>
                            {{else}}
                                <li><a href="/account/register">premium</a></li>
                            {{/if}}
                            <li><a href="about.html">About</a></li>
                            <li><a href="contact.html">Contact</a></li>
                        </ul>
                    </div>
                    <!-- Nav End -->
                </div>

                <div class="top-meta-data d-flex align-items-center">
                    <!-- Top Search Area -->
                    <div class="top-search-area">
                        <form action="" method="get">
                            <input type="search" name="SearchText" id="topSearch" placeholder="Search and hit enter...">
                            <button type="submit" class="btn" formaction="/post/search"><i class="fa fa-search" aria-hidden="true"></i></button>
                        </form>
                    </div>
                    <!-- Login -->
                    <form id="frmLogout" action="/account/logout" method="POST"></form>
                    {{#if lcIsAuthenticated}}
                        <div class="dropdown">
                        <button class="btn btn-secondary dropdown-toggle user-name" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                              <b>{{lcAuthUser.UserName}}!</b>
                        </button>
                        <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
                            <a class="dropdown-item" href="/account/profile/{{lcAuthUser.UserID}}">Profile</a>
                            <a class="dropdown-item" href="javascript: $('#frmLogout').submit()">
                                <i class="fa fa-sign-out" aria-hidden="true"></i>
                                Logout
                            </a>
                            <a class="dropdown-item" href="/account/reset/{{lcAuthUser.UserID}}">Reset Password</a>
                            {{#if (isWriter lcAuthUser)}}
                                <a href="/writer/write" class="dropdown-item">Submit post</a>
                            {{/if}}
                            <a class="dropdown-item" href="#">Something else here</a>
                            
                        </div> 
                        </div>
                    {{else}}
                        <a href="/account/login" class="login-btn"><i class="fa fa-user" aria-hidden="true"></i></a>
                        <a href="/account/register" class="login-btn"><i class="fa fa-plus" aria-hidden="true"></i></a>
                    {{/if}}
            {{#if lcIsAuthenticated}}
                {{#if (isWriter lcAuthUser)}}
                    <a href="writer/posts" class="submit-post"><span><i class="fa fa-cloud-upload"></i></span> <span class="video-text">Submit Post</span></a>
                {{/if}}
            {{/if}}
                
                    
                </div>
            </div>
        </nav>
    </div>
</div>

<style>
    .user-name {
        margin: 0 10px;
    }
</style>